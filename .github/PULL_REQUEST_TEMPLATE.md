## What

<!-- One or two sentences: what does this PR change? -->

## Why

<!-- The problem it solves or the behavior it fixes. Link the issue if there is one. -->

## Tests

<!-- What did you add or change under src/**/__tests__/? For anything shaped like
     (input) => decision, list the adversarial inputs you covered.
     If you verified something manually against a real model or backend, say what. -->

- [ ] `npm run typecheck` passes
- [ ] `npm test` passes
- [ ] `npm run build` passes
- [ ] Tests need no network, API key, or model download

## Checklist

- [ ] Every commit is signed off (`git commit -s`), certifying the [Developer Certificate of Origin](https://developercertificate.org)
- [ ] No private URLs, internal document paths, or personal names in code, comments, or docs
- [ ] If this adds a supported embedder, its measured constants are in this PR
